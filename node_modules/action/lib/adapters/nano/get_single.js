var get_single = function(conString,table,params,callback){
	//tes erwet
	var nano = require('nano')(conString);
	var db = nano.use(config.db.database_name);

	var lErr = [];
	var options = {
		id: params.id || null
	}
	
	console.log(options);
	console.log(table);
		
	var the_query = function(){
		var value = '';
		db.get(options.id, {_id:options.id}, function(err, content){
			console.log(err);
			console.log(content);
			callback(null,content);
		});  		
	}
	
	if(options.id == null){
		lErr.push({'application error':'the "id" query string must be specified'});
		client.end(); 
		callback(lErr,null);
	} else {
		the_query();		
	}
}

module.exports = get_single;
